class_name Interactable extends Area2D

export var interact_label = "none" #text popup when you have the option to interact with something
export var interact_type = "none" #class of what the interaction will be
export var interact_value = "none" #actual value of the interaction


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
